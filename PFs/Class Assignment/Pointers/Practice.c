#include <stdio.h>

// Exercise 1: C Program to Add Two Numbers Using Pointer

/* int main(){
	int *pnum1, *pnum2;
	int num1, num2, sum;

	printf("Enter two numbers: ");
	scanf("%d %d", &num1, &num2);

	pnum1 = &num1;
	pnum2 = &num2;
	sum = *pnum1 + *pnum2;

	printf("Sum = %d",sum);

	return 0;
} */

// Exercise 2: C Program to Access Array Elements Using Pointer

/* int main(){
	int data[5];

	printf("enter elements: ");

	for (int i = 0; i < 5; ++i){
		scanf("%d", data + i);
	}

	printf("you entered: \n");

	for (int j = 0; j < 5; ++j){
		printf("%d \n", *(data + j));
	}

} */

// Exercise 3:
